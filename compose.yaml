services:
  portfolio:
    image: ghcr.io/spitzerl/portfolio:latest
    container_name: portfolio
    restart: unless-stopped
    expose:
      - "80"
    networks:
      - proxy

networks:
  proxy:
    external: true
